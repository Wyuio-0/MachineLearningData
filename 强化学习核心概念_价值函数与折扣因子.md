# 强化学习核心概念：价值函数与折扣因子

本文档是对强化学习（Reinforcement Learning）中三个极其核心的概念的详细拆解：**折扣因子**、**状态价值函数（V函数）** 和 **动作价值函数（Q函数）**，并对部分教材中关于 AlphaGo 的不严谨表述进行了探讨与勘误。

---

## 1. 为什么要有个“折扣因子” (Discount Factor)？

**现实逻辑**：未来的钱不如现在的钱值钱（利息、通货膨胀），未来的奖励也不如现在的奖励实在（存在不确定性）。

**数学体现**：在价值函数的公式中体现为参数 $\gamma$（Gamma）。$\gamma$ 通常是一个介于 $0$ 和 $1$ 之间的小数（例如 $0.9$）。
* 走第一步马上拿到的奖励，不打折（$\gamma^0 = 1$）。
* 走第二步才拿到的奖励，打 $9$ 折（$\gamma^1 = 0.9$）。
* 走第三步拿到的奖励，打 $8.1$ 折（$\gamma^2 = 0.81$）。

**核心作用**：它能促使智能体（Agent）变聪明——**尽可能快地**去完成任务和获取奖励，而不是在原地无意义地徘徊和无限期拖延。

---

## 2. 状态价值函数 $V^\pi(s)$ 

> **一句话解释**：给“当前所处的环境/局面”打分。
> **回答的问题**：“如果你现在处于状态 $s$，你未来总共能拿到多少分？”

**公式**：
$$V^\pi(s) = E_\pi [G_t | s_t = s] = E_\pi \left[ \sum_{k=0}^\infty \gamma^k r_{t+k+1} | s_t = s \right]$$

**公式拆解**：
* `$s$`：状态 (State)。也就是现在的环境局面。
* `$\pi$` (Pi)：策略 (Policy)。可以理解为 AI 当前脑子里的“做事习惯”或“行动指南”。
* `$G_t$`：回报 (Return)，即未来拿到的所有奖励的“折现总和”。
* `$E_\pi[\dots]$`：**期望值**。因为未来有很多种随机性，所以要在策略 $\pi$ 的指导下算一个**平均预期收益**。

**通俗案例（下象棋）**：
$V^\pi(s)$ 评估的是**当前的棋盘局面**。如果马上要把对方将死了，这个局面的 $V$ 值就非常高；如果您的“帅”被包围了，$V$ 值就很低。

---

## 3. 动作价值函数 $Q^\pi(s, a)$ (Q函数)

> **一句话解释**：给“此时此刻的某个具体选择”打分。
> **回答的问题**：“在现在的状态 $s$ 下，如果你第一步**强制选择做出动作 $a$**，未来总共能拿到多少分？”

**公式**：
$$Q^\pi(s,a) = E_\pi [G_t | s_t = s, a_t = a] = E_\pi \left[ \sum_{k=0}^\infty \gamma^k r_{t+k+1} | s_t = s, a_t = a \right]$$

**公式拆解**：
* 对比可见，Q函数的公式和 V函数 几乎一模一样。唯一的区别是竖线后面的条件多了一个 `$a_t = a$`。
* 意思是：不管你平时的做事习惯（策略 $\pi$）是什么，**强制要求你这一步必须做出动作 $a$**，做完这一步之后，再恢复你平时的习惯 $\pi$ 接着走，算出这样走的总期望收益。

**通俗案例（下象棋）**：
在当前的棋盘局面下（状态 $s$），您可以“跳马”，也可以“出车”。
* $Q(s, \text{跳马})$ 算出来的就是跳马的具体最终收益。
* $Q(s, \text{出车})$ 算出来的就是出车的具体最终收益。

---

## 4. 💡 核心总结与对比

| 对比项 | 状态价值函数 V(s) | 动作价值函数 Q(s, a) |
| :--- | :--- | :--- |
| **评估对象** | 评估当前的“处境” | 评估在处境下的“具体决策” |
| **通俗话语** | “我现在这把牌好不好？” | “我现在这把牌，打出一对王炸到底好不好？” |
| **实战意义** | 用于评估环境的整体优劣 | 极其关键！机器算出各种动作的Q值后，只需挑选Q值最大的去执行即可，这也是著名算法 Q-Learning 的基石。 |

---

## 5. 探讨与勘误：AlphaGo 是纯免模型强化学习吗？

在一些入门教材（如《Easy RL》第一章）中，有时会将 AlphaGo 系列简单粗暴地归类为“都采用免模型（Model-Free）强化学习”。**这种表述是不严谨甚至具有极强误导性的。**

### “免模型”与“基于模型”的本质区别
* **免模型 (Model-Free)**：智能体不知晓环境运行的真实规则，只能通过不断与环境交互、试错来死记硬背经验（如 Q-Learning、PPO）。
* **基于模型 (Model-Based)**：智能体已知（或自己学习到了）环境的完整规则。在行动前，它能在脑海中进行环境推演（规划），精确预测未来的走向。

### AlphaGo 的真实架构：完美的混合体
在严谨的学术讨论中，AlphaGo 被公认为是一个 **Model-Based（基于模型）的规划搜索 与 Model-Free（免模型）的深度强化学习 完美结合的混合系统**：

1. **基于模型 (Model-Based) 的核心决策：**
   AlphaGo 下棋时依赖的核心算法是**蒙特卡洛树搜索 (MCTS)**。MCTS 是一种非常典型的规划算法，它必须依靠完全已知的环境模型（围棋吃子规则）来在脑海中向后推演千万步未来。**如果没有环境模型，MCTS 根本无法运作。**
2. **免模型 (Model-Free) 的辅助网络训练：**
   为了缩小 MCTS 的搜索范围，AlphaGo 训练了“策略网络”（提供走子直觉）和“价值网络”（也就是上文提到的 $V$ 函数，用于一眼评估当前局面的胜率）。在**训练**这两个神经网络时，DeepMind 采用了策略梯度（Policy Gradient）等免模型的强化学习算法。

**结论**：教材之所以会写“AlphaGo 采用免模型”，往往是因为作者的视角仅局限在了它“如何训练其内部的神经网络”上。但从 AlphaGo 对弈时的整体决策架构来看，它依靠确定的围棋规则进行前瞻推演的能力，正是**基于模型强化学习**最经典的展现。在深入学习理论时，应保持独立思考与批判精神。
